export type PlanoVersion = {
    id_version: string
    id_plano: string
    nro_version: number
    estado: string
    comentarios: string
    creado_por: string
    created_at: string
    updated_at: string
}
